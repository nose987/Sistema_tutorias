# Este archivo sobrescribe la configuración de .env SOLO cuando se ejecutan las pruebas (APP_ENV=testing).

# 1. Configuración del Entorno
APP_ENV=testing
APP_DEBUG=true

# 2. Configuración de la Base de Datos (Clave para solucionar tu error)
# Usar SQLite en memoria es lo más rápido y seguro para las pruebas.
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# 3. Otros Servicios (para evitar que los tests interactúen con servicios reales)
# Esto es opcional, pero buena práctica:
# - Cambiar el mailer a 'array' para que los correos se capturen en la memoria.
MAIL_MAILER=array
# - Cambiar las colas a 'sync' para que las tareas se ejecuten inmediatamente.
QUEUE_CONNECTION=sync
# - Cambiar el sistema de caché a 'array' (en memoria).
CACHE_STORE=array
# - Cambiar el sistema de sesiones a 'array' (en memoria).
SESSION_DRIVER=array